jwt:
  public:
    key: classpath:app.pub
  private:
    key: classpath:app.key
spring:
  application:
    name: habitquest
  datasource:
    url: jdbc:postgresql://localhost:5432/habitquest
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
